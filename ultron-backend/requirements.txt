Flask>=3.0,<4.0
psutil>=6.0,<7.0
anthropic>=1.0.0,<2.0.0
pywin32>=306; platform_system=="Windows"
WMI==1.5.1; platform_system=="Windows"
# The container's server (see gunicorn.conf.py). Does not run on Windows, where
# local development keeps using `python app.py`.
gunicorn>=23.0,<24.0; platform_system!="Windows"
