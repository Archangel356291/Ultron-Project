# ultron-backend index

- [[CODE-AUDIT|CODE-AUDIT]] — living log of full-codebase audits (backend + bot). Latest pass (2026-09-13): connection tracking + beta spend cap added, no bugs found — verified live via `dev-tools/test_beta_spend_cap.py` (real Flask test client, scripted fake Anthropic client, asserts the actual refusal). Earlier pass same day: found and fixed a real bug — the Discord bot's start script never loaded `.env`, so its token had to be pasted into a git-tracked file.
- [[README|README]] — what the backend does: Flask API for CPU/memory/temp/Docker/disk/updates, primary target Windows 11, runs unmodified on the Raspberry Pi 400/Linux. Covers the `beta_tester` role and now also serves the dashboard itself at `/` (fixes a mobile-browser `file://` bug). Cost controls section now also covers the $1.00 lifetime beta spend cap (`ULTRON_BETA_MAX_SPEND_USD`) and the new admin-only `/api/connections` presence endpoint.
- [[BETA-INVITE-MESSAGE|BETA-INVITE-MESSAGE]] — formal, ready-to-send copy/paste invite for asking someone to beta test. The first ask, not the onboarding — send `BETA-TESTER-GUIDE` once they say yes.
- [[BETA-TESTER-GUIDE|BETA-TESTER-GUIDE]] — plain-language walkthrough written for the tester themselves (not a dev) — installing Tailscale, connecting, what they can/can't do, what to report. Hand this to people directly, don't just point them at the repo.
- [[BETA-TESTERS|BETA-TESTERS]] — credits roster for beta testers plus the add/remove process. Each tester now gets their own distinct token (`ULTRON_BETA_TOKENS`, name:token pairs) — independently revocable, attributed via `/api/whoami`. Roster empty for now. New "Spend cap" section: every beta tester is capped at $1.00 of real, lifetime spend (`ULTRON_BETA_MAX_SPEND_USD`), enforced in `/api/chat` itself.
- [[BETA-LAUNCH-CHECKLIST|BETA-LAUNCH-CHECKLIST]] — beta launch readiness tracker. Final pre-launch check (2026-09-13): everything re-verified live from cold — backend, concurrency, Discord bot, secrets. One real gap flagged: `ULTRON_BETA_TOKENS` is unset, so add a real tester before sending anyone a link.
- [[REMOTE-ACCESS|REMOTE-ACCESS]] — Tailscale remote-access walkthrough. Its grants example was fixed (2026-09-13) after being caught by a real save error — `autogroup:self` only works as a destination, not a source.
